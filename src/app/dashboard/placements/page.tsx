import { Card, CardContent, CardDescription, CardHeader, CardTitle } from "@/components/ui/card";

export default function PlacementsPage() {
  return (
    <Card>
      <CardHeader>
        <CardTitle className="font-headline">IT Placements</Car